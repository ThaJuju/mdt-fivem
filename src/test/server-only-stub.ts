/**
 * Remplaçant de `server-only` sous Vitest — voir `vitest.config.ts`.
 * Volontairement vide : le module réel n'existe que pour lever une erreur à
 * l'import depuis un composant client.
 */
export {};
